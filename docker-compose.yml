services:
  web:
    container_name: finance
    build: .
    init: true
    env_file: .env
    environment:
      FINANCE_DATA_DIR: /data
    volumes:
      - /srv/data/finance:/data
    restart: unless-stopped
